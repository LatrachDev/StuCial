
  # SC

  This is a code bundle for SC. The original project is available at https://www.figma.com/design/iYap9HukyuVltMs10vour7/SC.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  